[project]
name = "newt-starter-yam"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "newt @ git+ssh://git@github.com/new-theory-research/newt-python.git@db32a2442400bfa00b63322b0c345e2a707763fa",
    "numpy>=1.26",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["."]
