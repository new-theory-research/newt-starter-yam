[project]
name = "newt-starter-yam"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "newt @ git+ssh://git@github.com/new-theory-research/newt-python.git@be432b2b3cda4f2702850accb0e3ff1286b716b9",
    "numpy>=1.26",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["."]
